# AB Jewellery Makers - Luxury Showcase Application

A web application for **AB Jewellery Makers** (Indriya Jewellers), crafted with React and Vite. It features a luxury showcase of handcrafted jewellery, collection filters, detailed product views, and direct WhatsApp enquiry integration.

---

## 📋 Prerequisites

Make sure you have the following installed on your system before proceeding:

* **Node.js**: `v16.0.0` or higher (v18 / v20 recommended)
  * Check version: `node -v`
* **npm**: `v8.0.0` or higher (comes bundled with Node.js)
  * Check version: `npm -v`

> 💡 **Note**: This is a JavaScript / React project. Python virtual environments (`venv`) or Python activations are **not** required.

---

## ⚙️ Installation & Setup

Follow these steps to set up and run the application locally on your machine:

### 1. Open Terminal in Project Directory
Ensure your terminal / command prompt is located at the root of the project:
```bash
cd /path/to/ab-jewellery-makers
```

### 2. Install Project Dependencies
Run the package installer to download and set up all required dependencies (React, Vite, React Icons, etc.):
```bash
npm install
```

---

## 🚀 Running the Application

### Start Development Server
To launch the app in development mode with hot-module replacement (HMR):

```bash
npm run dev
```

Once started, open your web browser and navigate to:
```text
http://localhost:5173
```
*(or the local URL shown in your terminal output)*

---

## 🛠️ Available Scripts

In the project directory, you can run the following commands:

| Command | Description |
| :--- | :--- |
| `npm run dev` | Starts the development server at `http://localhost:5173`. |
| `npm run build` | Bundles the application into the `dist/` directory for production. |
| `npm run preview` | Serves the production build locally for testing prior to deployment. |

---

## 🧰 Tech Stack & Features

* **Framework & Build Tool**: React 18 & Vite
* **Styling**: Custom Modern Vanilla CSS with responsive luxury design tokens
* **Iconography**: React Icons (`react-icons`)
* **Key Features**:
  * Cinematic Hero Banner
  * Circular Collections & Category Filters
  * Interactive Product Detail Modal
  * Direct WhatsApp Enquiry Integration
  * Customer Reviews & Visual Gallery
